import React from 'react';

export interface NotFoundPageProps {}

const NotFoundPage: React.FC<NotFoundPageProps> = () => {
  return <p>Nie znaleziono lokalizacji</p>;
};

export default NotFoundPage;
